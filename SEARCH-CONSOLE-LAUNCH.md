# Search Console launch status — 2026-09-25

The `layerbylaya.com` Domain property was verified in the Chrome profile named Layer, signed into the LayeR Google account. Google verified ownership through a TXT record in the `layer-web` Vercel DNS zone. Keep that record in place to retain ownership.

`https://layerbylaya.com/sitemap.xml` was submitted. Its latest Search Console status is **Success**, with **17 discovered pages**. The live XML is valid, returns 200, and is also referenced in `robots.txt`.

After the brand assets went live, URL Inspection reported **the homepage is on Google and indexed**, served over HTTPS. A recrawl request for the updated favicon and logo was accepted into Google's priority crawl queue. Kerala bridal was unknown to Google; About, portfolio and Dubai bridal were discovered but not yet indexed. Indexing requests for those four pages were also accepted. Submission does not guarantee indexing or ranking, and repeated requests do not improve queue position.

Google search appearance and performance reports are still processing because this is a newly verified property. Review them after Google has crawled the site. The small search result icon comes from the favicon; the Organization logo structured data is a separate brand signal. Google may take several days to weeks to refresh a favicon.
